package first_last_sum;

public class FirstLastSum {
    public static int sumFirstAndLastDigit(int num) {
        // if (num < 0) {
        // return -1;
        // }
        // int lastInt = num % 10;
        // int firstInt = 0;

        // // To find the first digit of the number
        // while (num != 0) {
        // firstInt = Math.abs(num % 10);
        // num /= 10;
        // }

        // return lastInt + firstInt;
        return -1;
    }
}
